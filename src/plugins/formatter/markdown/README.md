# Markdown Formatter Plugin

## Description

Converts collected plugin summaries into a single Markdown document with YAML frontmatter, a replaceable marker block, a dated title, and per-input sections separated by horizontal rules.

## Configuration

- No options. The plugin accepts an empty config object.

## Basic config

```yaml
formatter:
  plugin: markdown
  # config: {}  # not required
```
