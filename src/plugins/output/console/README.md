# Console Output Plugin

## Description

Writes the formatted summary to stdout. Useful for quick local runs or piping into other tools.

## Configuration

- No options. The plugin accepts an empty config object.

## Basic config

he plugin accepts an empty config object.

## Basic config

```yaml
outputs:
  - plugin: console
  # config: {}  # not required
```
